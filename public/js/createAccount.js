const newAccount = async(event) => {
    event.preventDefault();

    const firstName = document.querySelector().value.trim();
    const lastName = document.querySelector().value.trim();
    const address = document.querySelector().value.trim();
    const unitNumber = document.querySelector().value.trim();
    const emailAddress = document.querySelector().value.trim();
    const password = document.querySelector().value.trim();
    const vehicleMake = document.querySelector().value.trim();
    const vehicleModel = document.querySelector().value.trim();
    const vehicleColor = document.querySelector().value.trim();
    const vehiclePlateNumber = document.querySelector().value.trim();

    if (/* All the above consts, need to check on correct order for importing for models*/)
}

document.querySelector('#createAccount').addEventListener('submit', newAccount);